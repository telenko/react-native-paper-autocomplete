import { StyleSheet } from "react-native";

export default StyleSheet.create({
  actionsContainer: {
    zIndex: 999,
    height: "100%",
    position: "absolute",
    top: 0,
    flexDirection: "row",
    alignItems: "center",
  }
});
