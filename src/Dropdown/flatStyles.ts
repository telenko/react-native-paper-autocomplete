import { StyleSheet } from "react-native";

export default StyleSheet.create({
  noItems: {
    paddingHorizontal: 10,
  },
  divider: {
    marginLeft: 16,
  },
  flatList: {
    maxHeight: 300,
  },
});
