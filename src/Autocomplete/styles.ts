import { StyleSheet } from "react-native";

export default StyleSheet.create({
  option: {
    height: 35,
    justifyContent: "center",
  },
  chipContainer: {
    flexDirection: "row",
    flexWrap: "wrap",
    marginTop: 5,
  },
  chip: {
    minWidth: 80,
    height: 40,
    alignItems: "center",
    marginRight: 5,
    marginBottom: 5,
  },
});
